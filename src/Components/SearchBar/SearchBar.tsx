import React from "react";
import classes from "./styles.module.scss";

const SearchBar = () => {
  return (
    <div className={classes.container}>
      <div className={classes.container_input}></div>
    </div>
  );
};

export default SearchBar;
